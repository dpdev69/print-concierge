BAMBUDDY_BASE_URL=http://127.0.0.1:8080
BAMBUDDY_API_KEY=replace-with-a-least-privilege-token

# V0/V1 safety defaults.
PRINT_CONCIERGE_DIRECT_START_ENABLED=false
PRINT_CONCIERGE_REQUIRE_CONFIRMATION=true
PRINT_CONCIERGE_STATE_DB=~/.print-concierge/state.sqlite3
PRINT_CONCIERGE_BAMBUDDY_MANUAL_START=true

# Optional JSON search backends. The response should include a `results` array
# with model fields such as id/title/url/license/profile/file.
# Public web model search is enabled by default through 3DSEARCH. Set this false
# only when you want Bambuddy/local archive search with no public web sources.
# PRINT_CONCIERGE_PUBLIC_WEB_SEARCH_ENABLED=true
# PRINT_CONCIERGE_PUBLIC_WEB_SEARCH_BACKEND=3dsearch
# PRINT_CONCIERGE_3DSEARCH_SEARCH_URL=https://3dsearch.net/?q={query}&lang=en
# Maximum direct public file import size in bytes. Default: 150 MB.
# PRINT_CONCIERGE_PUBLIC_IMPORT_MAX_BYTES=157286400
# Maximum time to wait for Bambuddy source slicing during public import.
# PRINT_CONCIERGE_PUBLIC_IMPORT_SLICE_WAIT_SECONDS=300
# Set backend=site-search to use a generic search page instead.
# PRINT_CONCIERGE_PUBLIC_WEB_SEARCH_SITES=makerworld,printables,thingiverse,thangs
# PRINT_CONCIERGE_PUBLIC_WEB_SEARCH_URL=https://duckduckgo.com/html/?q={query}
# PRINT_CONCIERGE_MAKERWORLD_SEARCH_URL=https://search.example/makerworld?q={query}
# PRINT_CONCIERGE_PRINTABLES_SEARCH_URL=https://search.example/printables?q={query}
# PRINT_CONCIERGE_EXTERNAL_SEARCH_PROVIDERS=[{"name":"thangs","url":"https://search.example/thangs?q={query}","result_path":"items"}]

# Keep broad Bambuddy MCP/API credentials out of the same agent profile that runs
# Print Concierge. This companion should only expose curated, confirmation-gated
# print tools.
